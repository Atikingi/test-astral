import EditView from '../../components/EditView';
import { inputFields } from '../../constants/inputFields';

const ProfilePage = () => {
  return <EditView inputsArray={inputFields} />;
};

export default ProfilePage;
